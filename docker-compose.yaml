services:
    llm:
        build:
            context: .
            dockerfile: Dockerfile
        hostname: llm
        env_file:
            - .env
        ports:
            - 8000:8000
        volumes:
            - ./faiss_index:/app/faiss_index
        restart: unless-stopped
